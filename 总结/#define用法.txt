宏定义

#define 用于替换字符串 用于main函数外
用法: #define N 100 
     将N用100代替

终止函数用#undef

注意: 1) #define 后面不用分号
      2) 若被" "包围则不被替代 如printf函数
      3) 宏定义允许嵌套
      4) 替换的表达式最好带有()避免带入时计算顺序错误 如 #define SQ(y) (y)*(y)


宏定义与函数的区别:宏定义只会原样带入 but 函数会进行计算 and 占用内存 and 可以重复调用